# 实验监督记录

本文件记录主实验、消融和外部评测的启动、阶段进度、异常与完成时间。逐次运行的原始
终端输出、GPU 快照和结构化状态保存在 `results/runs/<run-name>/logs/`，不在本文件重复。

## 2026-08-20

- 严格 K=3 缓存已迁移为 `cache/patch_embeddings_k3_2s_8fps/packed_v1/`：22,820 条目、
  718 个 shard，无旧单视频缓存或临时文件残留。
- 首次完整开发集主实验 `alpha_stall` 使用 `cuda:0,cuda:1` 运行时，ComGenVid 与
  VideoFeedback 已完成；GenVideo evaluation 至 10,608/13,623（77.9%）后，两个 worker
  长时间阻塞于机械盘 I/O。该次运行已停止、两张 GPU 显存已释放，未产生可用完整指标。
- 已改为 shard 整体内存读取后重新运行；运行命令和实时状态见
  `results/runs/alpha_stall/command.txt`、`progress.json` 与 `logs/run.log`。监督频率为约
  10 分钟一次，快照追加到 `results/runs/alpha_stall/logs/monitor.log`。
- 重跑已完成三套开发集的缓存评分与标准 CSV 写入：共 21,421 条唯一逐视频分数、58,498 条
  窗口分数。运行在 paired bootstrap 阶段中断，故 `alpha_stall` 仍为 `interrupted`，不可冻结。
  初步核查发现两级经验 CDF 使 Global 分支严重饱和，当前结果仅作诊断用途，不作为论文结果。
- 已定位并修复饱和的数值根因：高维经验协方差的病态尾部被白化过度放大。主配置改用
  real-only OAS 收缩；在 VideoFeedback 的真实校准缓存上完成 GPU 健康检查后，四个分支白化
  矩阵均有限。下一次 `alpha_stall` 将以该配置从头完整重跑。
- OAS 重跑在 ComGenVid evaluation 进行到 2,016/4,298 时主动停止，用于修复机械盘读取瓶颈；
  该运行不产生可用结果。观察到 `/dev/sda` 100% 利用率而双 GPU 间歇空闲，故后续重跑使用
  连续且互斥的 shard 区间分配给独立双卡 worker 的新调度路径。曾短暂测试主进程 RAM 内线程
  分发，但完整运行未能稳定完成首个 shard，已停止且不作为正式路径。
- 最终 shard 亲和重跑已完成三套开发集评分：ComGenVid 4,298 条、VideoFeedback 3,500 条、
  GenVideo 13,623 条；`video_scores.csv` 共 21,421 条、无重复、无 NaN。首次在 bootstrap 的
  64 位种子兼容性错误处中断，修复后通过 `run_alpha_stall.sh --resume-bootstrap` 成功补齐 1,000
  次 paired bootstrap，`alpha_stall` 状态已更新为 `completed`，两张 GPU 已释放。
