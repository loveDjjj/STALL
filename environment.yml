name: stall
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - ffmpeg                  # required by video_index.py (ffprobe)
  - pip
  - pip:
    - numpy
    - opencv-python
    - Pillow
    - scikit-learn
    - pandas
    - tabulate
    - tqdm
    - datasets
    - huggingface_hub
    - pyarrow
    - matplotlib
