# 实验结果分析

本目录保存已完成运行的指标解读和跨实验结论。原始逐视频分数、窗口分数、指标表和
bootstrap 结果始终以 `results/runs/<run-name>/` 中的标准 CSV 为准。

## 待完成实验

| 阶段 | 运行名或入口 | 状态 |
|---|---|---|
| 完整开发集方法 | `alpha_stall` | 读取层修复后待重跑 |
| 结构与时间覆盖消融 | `run_ablation.sh` | 待运行 |
| 校准稳定性 | `run_calibration.sh` | 待运行 |
| 外部泛化 | `run_external.sh genvidbench` | 待运行 |

## 运行时观察

当前严格特征缓存位于机械盘 `/data`。首轮双卡评分在 GenVideo 的 77.9% 进度发生
`mmap` 缺页导致的长时间磁盘 I/O 阻塞，故该轮没有有效结果。读取层现改为完整 shard
内存 LRU：每个 GPU worker 最多保留两个 shard，剩余内存交由操作系统用于文件页缓存。
重跑完成后再记录正式吞吐与指标。
