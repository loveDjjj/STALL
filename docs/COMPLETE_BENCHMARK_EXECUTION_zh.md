# 23单元完整论文实验执行记录

状态：23单元实验与区间已完成并验收；官方单视频默认前向与真实配对对齐也已完成。用户要求的主实验及全部消融均补为23单元；这替代此前“消融保留20单元”的方案，不替代已验收20单元历史数据。下文进度段保留时间顺序，以末节与当前运行状态为准。

## 完成条件

- 恢复GenVideo HotShot/MoonValley和VideoFeedback Hotshot-XL，保留原始身份及元数据。
- 8帧fake与real使用同一1秒规则，禁止重复填帧；官方复现保持官方NPZ。
- 主实验、组件、表示、参考来源、均值/白化/对角、观察、单分支输出及配对指标全部23单元覆盖。
- 各配置使用相同评价身份；分数模型改变时各自重评分参考；短片观察配置退化一致。
- 分析原文与复现差异：动态等级、样本池、身份、时长、AP与汇总，不能为贴近原文调分数。
- 保留旧20单元结果，单列新23单元和任何筛选变化；完整导出、源组区间、操作点及数据身份验收后更新论文。

## 已完成工作

1. 从HF镜像取得官方VideoFeedback train/test标注，恢复动态等级与原始URL。
2. 当前300条/生成器的元数据全部能匹配；并非全部等级3/4。Pika只有132条属于3/4，AnimateDiff219条。
3. CPU诊断保留等级3/4、两真实源等额配对后，STALL单窗域平均由0.857156/0.869419变为0.849273/0.862656；完整方法由0.838391/0.852220变为0.831982/0.846148。
   该诊断同时改变fake子集与匹配real，不是单个因素的因果分解，也不是原文完整池复现。
4. 增加独立8帧单窗评分Mixin及共享实验入口；不改16帧路径。相关7项测试通过，仍需原视频GPU验收。
5. GenVideo归档与VF归档恢复进行中；两个目标域的8帧CDF共享评分已启动。

### 进展追加

- GenVideo完整归档MD5为812e14ee004a32e5bf0e14082065f03d，与官方文档一致；1326条短视频已恢复并核验成员CRC/SHA256。
- GenVideo新增8帧评价清单2026唯一身份、2652配对行已经生成；真实裁剪身份带duration1后缀，源组仍沿原身份，避免与16帧分数冲突。
- VF测试归档恢复60个合格Hotshot成员；训练归档改为HTTP Range定向读取2677个合格成员。已验证Range边界与CRC后停止无关整包下载，部分归档仍留待任务完成后处理。
- 10项短视频、官方索引与表格相关测试通过。两卡CDF后续队列已启动：GenVideo依次评价/官方/阈值；VF依次阈值/待恢复完成后评价/官方。
- 尚未有新的23单元检测结果，不以恢复数量或CDF进度替代完成验收。

### 第二阶段进展

- 三个缺失生成器全部恢复：GenVideo1326，VF合格Hotshot2737（训练2677＋测试60）。VF均为8 FPS、8帧、1秒；原文2736可与两真实来源等配额时对奇数fake截尾的规则相容，不声称一条视频损坏。
- VF新增评价固定500 real＋500 fake；GenVideo为700真实裁剪与1326 fake，覆盖两个单元。所有消融共享这些身份，不能把覆盖完整写成样本规模已与原文完全相同。
- 8帧CDF两域各2000已完成；GenVideo全部消融共享评分已完成。
- GenVideo短单元初值：HotShot官方0.788636/0.776709、完整0.823871/0.819732；MoonValley官方0.726662/0.764143、完整0.881986/0.901826。
- 12个真实/生成、8/16帧片段的官方前向诊断完成：相同特征评分全部精确一致；batch8和上游实际batch32前向的样本最大最终分数差约0.000088。不是全数据数值差异上界。
- ComGenVid公布身份核对：fake两类各1700全部匹配；当前898 real均在官方1698唯一名单内，另外200用于fit，600未纳入当前fit/eval。来源审计为文件名集合，不冒充内容hash证明。
- 500条VF真实视频全部匹配官方real元数据，动态等级均为4。故筛选排除实际作用于fake，real变化来自配对再平衡。
- 完整23单元CPU汇总/区间队列已等待GPU任务，生成coverage_only与paper_filter两个明确协议；所有观察配置的短片段退化为相同单窗。

## 资产与入口

- 元数据/下载：datasets/recovery/metadata、datasets/recovery/archives。
- 短视频参考清单：data/manifests/short_video/vatex。
- 8帧实验：python -m evaluation.short_video_study，显式domain/role/device；run名complete23_*_short8。
- 动态等级诊断：results/runs/videofeedback_dynamic_filter_audit；代码evaluation/dynamic_filter_audit.py。

## 最终验收

两协议均已完成37个配置表行（含10个单分支输出）的23单元覆盖、19组1000次源组区间、8/16帧独立阈值和480次计时记录。新增11026条共享证据记录验收通过；所有复用的长窗分数逐条不变。52项测试通过，4063条恢复视频SHA256最终复核通过。

仅补覆盖的完整方法Macro-3为0.877264/0.880074；原文动态筛选协议为0.875322/0.878233，配套同池官方STALL为0.832306/0.838047。正式结果见results/paper_complete；原范围对照见results/runs/complete23_coverage_only。

主文稿已更新23单元主表、域级消融、摘要和限制。Local相对同预算Global的AUC区间为正，但AP区间略跨零，已收窄结论。相同生成器覆盖不等于原文逐视频池完全相同；动态筛选、文件名交集、实际前向差异均已有独立核验。

## 官方单视频默认前向对齐（执行记录，已完成）

为进一步减少工程差异，已启动原版STALL的batch32上限、不补尾批重评分，覆盖两个完整协议的全部16059片段身份（13033长窗＋3026短窗），两GPU按rank0/1划分。8帧/16帧分别输出complete23_native_stall_8、complete23_native_stall_16。

该路径与官方单视频inference入口一致：每个视频实际8或16帧，并不声称复现官方批量CLI的跨视频分组。12片段审计已证明我们的此路径与上游实际_embed_flat_frames特征逐元素一致。仍不能据此恢复未公开的原文历史缓存/批组/视频身份。

参数NPZ、帧索引和评价身份不改变。完成后需要比较与batch8对照的逐视频/逐单元差异，更新相应官方行及区间，重新做最终验收；不能因其他实验已完成而跳过此追加任务。

## 最终对齐结果

官方默认前向16259片段身份已完成，同池逐视频batch差异已核验；VF真实配对与官方函数逐身份一致。最终paper_aligned完整模型Macro-3为0.874472/0.877075，官方STALL为0.832295/0.837737。所有37表格配置行均23单元，19组区间重算，操作点和验收完整。上面0.875322/0.878233是中间旧配对诊断，保存在results/runs/complete23_filtered_legacy_pairing；正式results/paper_complete已替换为最终对齐版本。Local两项增量区间均为正，但AP下界接近零，正文已更新。所有GPU/CPU队列结束，无待跑任务。
